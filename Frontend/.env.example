# Not required. API URL is in constant.ts (DEPLOYED_API_ORIGIN).
# Site URL on Vercel uses VERCEL_URL automatically.
#
# Optional: if you use a custom domain for the frontend, set:
# NEXT_PUBLIC_BASE_URL=https://www.yoursite.com
